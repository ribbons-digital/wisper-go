{
  "name": "wispergo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "desktop:dev": "./scripts/desktop-dev.sh",
    "desktop:build": "./scripts/desktop-build.sh",
    "desktop:trust-cert": "./scripts/ensure-local-codesign-cert.sh && ./scripts/trust-local-codesign-cert.sh",
    "check:macos-thin-bundle": "./scripts/check-macos-thin-bundle.sh",
    "test": "pnpm --dir apps/desktop test && cargo test --workspace",
    "test:ts": "pnpm --dir apps/desktop test",
    "test:rust": "cargo test --workspace",
    "check:release-workflows": "./scripts/check-github-workflows.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
