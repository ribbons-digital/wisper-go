[package]
name = "wispergo-desktop"
version = "0.1.0"
description = "Local-first desktop dictation"
authors = ["Ribbons Digital"]
edition = "2021"

[lib]
name = "wispergo_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
